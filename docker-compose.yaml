version: "3.8"

services:
  # Frontend Service
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: frontend
    ports:
      - "3500:3000"
    restart: unless-stopped
  # Backend Service
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: backend
    ports:
      - "7500:7000"
    env_file:
      - ./server/.env
    restart: unless-stopped
